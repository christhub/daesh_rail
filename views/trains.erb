<h2 class="text-center">Trains!</h2>

  <% if @trains.any? %>
    <table class="table table-striped">
      <tr>
        <th>Train Name</th>
        <th>Origin City</th>
        <th>Final Destination</th>
        <th>Departure Time</th>
        <th>Stops</th>
        <th></th>
        <th></th>
      </tr>
    <% @trains.each() do |train| %>
    <tr>
      <td>
        <%= train.train_name %>
      </td>
      <td>
        <%= train.origin_city%>
      </td>
      <td>
        <%=train.final_destination_city%>
      </td>
      <td>
        <%=train.departure_time%>
      </td>
      <td>
        <%=train.stops%>
      </td>
      <td>
        <button class="btn btn-primary"><a style="color: #fff" href="/employee/modify_train/<%= train.id %>/">Update</a></button>
      </td>
      <td>
        <form action="/employee/delete_train/<%= train.id() %>/" method="post">
          <input name="_method" type="hidden" value="delete">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </td>
    <tr>
    <% end %>
    </table>
  <% else %>

    There are no trains, there may have been an incident. Check Al-Jazeera for details.

  <% end %>

<a href="/">Go Back</a>
